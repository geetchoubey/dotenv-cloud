# typed: false
# frozen_string_literal: true

# Homebrew formula for dotenv-cloud. Generated by the release pipeline from
# registry/homebrew-formula.rb.tmpl; manual edits are overwritten each release.
class DotenvCloud < Formula
  desc "Extends dotenv with secure remote secret resolution via provider plugins"
  homepage "https://github.com/geetchoubey/dotenv-cloud"
  version "__VERSION__"
  license any_of: ["MIT", "Apache-2.0"]

  on_macos do
    on_arm do
      url "__BASE__/dotenv-cloud-__TAG__-aarch64-apple-darwin.tar.gz"
      sha256 "__MAC_ARM_SHA__"
    end
    on_intel do
      url "__BASE__/dotenv-cloud-__TAG__-x86_64-apple-darwin.tar.gz"
      sha256 "__MAC_X86_SHA__"
    end
  end

  on_linux do
    on_arm do
      url "__BASE__/dotenv-cloud-__TAG__-aarch64-unknown-linux-gnu.tar.gz"
      sha256 "__LIN_ARM_SHA__"
    end
    on_intel do
      url "__BASE__/dotenv-cloud-__TAG__-x86_64-unknown-linux-gnu.tar.gz"
      sha256 "__LIN_X86_SHA__"
    end
  end

  def install
    bin.install "dotenv-cloud"
  end

  test do
    assert_match version.to_s, shell_output("#{bin}/dotenv-cloud --version")
  end
end
